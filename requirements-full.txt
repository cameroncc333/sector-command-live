# Full requirements — GitHub Actions daily engine + dashboard.
requests>=2.31.0
flask>=3.0.0
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.40
gspread>=6.0.0
google-auth>=2.30.0
beautifulsoup4>=4.12.0
scipy>=1.11.0
alpaca-trade-api>=3.0.0
# google-generativeai removed — llm_router uses direct REST API (no SDK needed)

# --- OPTIONAL: FinBERT news/FOMC scoring (heavy ~2GB with torch) ---
# Only enable on a runner with enough disk/RAM, or run locally.
# transformers>=4.40.0
# torch>=2.2.0

# --- OPTIONAL: RL inference (only if you wire load_rl_signal to live models) ---
# stable-baselines3>=2.3.0
# gymnasium>=0.29.0
